const actualPrice = 4000;
const tip = 500;
const finalPrice = actualPrice + tip;
const frensCount = 8;
const toPay = finalPrice / frensCount;
console.log(`С каждого по ${toPay} рублей, общий счёт равен ${finalPrice}, чаевые - ${tip}`)